import variantsCode from './variants.mdx?raw'
import colorsCode from './colors.mdx?raw'
import fullWidthCode from './fullwidth.mdx?raw'
import sizeCode from './sizes.mdx?raw'

# Button

It's a button.

-----
## Variants

The Button component has 4 variants - `default`, `solid`, `text`, and `link`.

<ComponentPreview code={variantsCode} />

-----
## Colors

The button component has 6 colors - `default`, `primary`, `info`, `warning`, `error`, and `success`.  
Link buttons do not support colors; they are always blue.

<ComponentPreview code={colorsCode} /> 

-----
## Sizes

Buttons come in 3 sizes: `sm`, `md`, `lg`

<ComponentPreview code={sizeCode} />
-----
## Full width

Full width buttons fill their container.

<ComponentPreview code={fullWidthCode} />
